{
  "name": "@aricma/itemids",
  "version": "1.0.0",
  "description": "An object to simplify state creation and state updates.",
  "main": "src/index.js",
  "scripts": {
    "test": "jest --watch",
    "coverage": "jest --collect-coverage",
    "ci-test": "jest --ci",
    "ci-coverage": "jest --ci --collect-coverage && npm run codecov",
    "codecov": "bash <(curl -s https://codecov.io/bash)"
  },
  "author": {
    "name": "Adrian Mindak",
    "email": "adrian@aricma.com",
    "url": "aricma.com"
  },
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.9.0",
    "@babel/preset-env": "^7.9.5",
    "babel-jest": "^25.4.0",
    "jest": "^25.4.0"
  }
}
